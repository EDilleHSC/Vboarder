[pytest]
addopts = --tb=short
# Avoid recursive/infinite backup folders and common large data dirs during test collection
norecursedirs = .* venv .venv backups SEC/backups logs api/scripts agents/tools agents/agent_runtime archive backup flowise_data __trash__ __temp
